const numberOfFilms = prompt("Сколько фильмов вы уже посмотрели?", "0");

const personalMovieDB = {
    count = numberOfFilms,
    movies = {},
    actors = {},
    genres = [],
    privat = false
};
